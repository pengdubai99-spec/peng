// This schema has been moved to packages/database/prisma/schema.prisma
// Use that as the single source of truth.
// This file is kept only to allow prisma CLI commands to work from this directory.
// To run migrations, use: cd ../../packages/database && pnpm prisma:migrate

// Re-export the shared schema:
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ⚠️ SHARED SCHEMA — Edit packages/database/prisma/schema.prisma instead!
// See: ../../packages/database/prisma/schema.prisma for all models
